*~
root/Home/*
*.swp
tags
.DS_Store

# Release build output
/dist/
clogs/*.gem
tmp/fuzz

# Local bundler config (e.g. `bundle config set --local with fox`)
.bundle/

# The Qt backend's shim is built, not committed (clogs/ext/qt/build.sh)
clogs/ext/qt/*.so
clogs/ext/qt/*.dylib
